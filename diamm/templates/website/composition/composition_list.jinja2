{% extends "base.jinja2" %}

{% block body %}
    <h1>Composition List</h1>
    <ul>
    {% for composition in content.results %}
        <li><a href="{{ composition.url }}">{{ composition.title }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}
