<div>
    <p class="upper-info">
        {% if result.role_ss %}
            Role{{ result.role_ss | length | pluralize }}:
            {% for role in result.role_ss %}
            {{ role }}{{ ", " if not loop.last else "" }}
            {% endfor %}
        {%  endif %}
    </p>

</div>
