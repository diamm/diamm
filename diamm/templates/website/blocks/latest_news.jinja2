{% for article in news %}
    <article class="news-story">
        <h4 class="title is-5">
            <a href="{{ pageurl(article) }}">{{ article.title }}</a>
        </h4>
        {{ article.summary | richtext }}
    </article>
{% endfor %}
