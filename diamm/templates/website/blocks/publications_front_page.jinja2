{% for publication in publications %}
<article>
    <h4 class="title is-5">
        <a href="{{ pageurl(publication) }}">{{ publication.title }}</a>
    </h4>
    <p>{{ publication.teaser }}</p>
</article>
{% endfor %}